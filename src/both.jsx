import React,{useEffect,useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import'./style.css';

const BASE='https://fapi.binance.com';
const n=v=>Number(v)||0;
const fmt=v=>n(v).toLocaleString('en-IN',{maximumFractionDigits:8});
const pct=v=>`${n(v)>=0?'+':''}${n(v).toFixed(2)}%`;
async function get(path){const r=await fetch(BASE+path,{cache:'no-store'});if(!r.ok)throw Error(`Binance ${r.status}`);return r.json()}
function ema(values,p){if(!values.length)return 0;const k=2/(p+1);let e=n(values[0]);for(let i=1;i<values.length;i++)e=n(values[i])*k+e*(1-k);return e}
function atr(k,p=14){if(k.length<p+1)return 0;let s=0;for(let i=k.length-p;i<k.length;i++){const h=n(k[i][2]),l=n(k[i][3]),pc=n(k[i-1][4]);s+=Math.max(h-l,Math.abs(h-pc),Math.abs(l-pc))}return s/p}
function analyse(k1,k5,k15,t){const c1=k1.map(x=>n(x[4])),c5=k5.map(x=>n(x[4])),c15=k15.map(x=>n(x[4]));const v1=k1.map(x=>n(x[5])),v5=k5.map(x=>n(x[5]));const e1=ema(c1,9),e5=ema(c5,9),e15=ema(c15,9);const up1=c1.at(-1)>e1&&c1.at(-1)>c1.at(-2),up5=c5.at(-1)>e5&&c5.at(-1)>c5.at(-2),up15=c15.at(-1)>e15&&c15.at(-1)>c15.at(-2);const dn1=c1.at(-1)<e1&&c1.at(-1)<c1.at(-2),dn5=c5.at(-1)<e5&&c5.at(-1)<c5.at(-2),dn15=c15.at(-1)<e15&&c15.at(-1)<c15.at(-2);const av=a=>a.length?a.reduce((x,y)=>x+y,0)/a.length:0;const spike=av(v5.slice(-11,-1))?v5.at(-1)/av(v5.slice(-11,-1)):0;const score=Math.round(Math.min(Math.abs(t.changePercent)*8,35)+Math.min(spike*12,30)+(up5&&up15?35:dn5&&dn15?35:0));const buy=up1&&up5&&up15&&t.changePercent>0&&spike>=1.5;const sell=dn1&&dn5&&dn15&&t.changePercent<0&&spike>=1.5;return{score,spike,signal:buy?'BUY':sell?'SELL':'WATCH',trend5:up5?'BULLISH':dn5?'BEARISH':'WAIT',trend15:up15?'BULLISH':dn15?'BEARISH':'WAIT',atr5:atr(k5),atr15:atr(k15)}}

function App(){
 const[universe,setUniverse]=useState([]),[tickers,setTickers]=useState({}),[rows,setRows]=useState([]),[busy,setBusy]=useState(true),[err,setErr]=useState(''),[last,setLast]=useState(null),[mode,setMode]=useState(()=>localStorage.getItem('engine-mode')||'PAPER'),[risk,setRisk]=useState(()=>Number(localStorage.getItem('engine-risk')||'0.5')),[maxPos,setMaxPos]=useState(()=>Number(localStorage.getItem('engine-maxpos')||'3')),[signals,setSignals]=useState([]);
 const load=async()=>{try{setBusy(true);const[e,t]=await Promise.all([get('/fapi/v1/exchangeInfo'),get('/fapi/v1/ticker/24hr')]);const u=e.symbols.filter(s=>s.contractType==='PERPETUAL'&&s.quoteAsset==='USDT'&&s.status==='TRADING').map(s=>s.symbol);const map={};t.filter(x=>u.includes(x.symbol)).forEach(x=>map[x.symbol]=x);setUniverse(u);setTickers(map);setLast(new Date());setErr('')}catch(e){setErr(e.message)}finally{setBusy(false)}};
 useEffect(()=>{load();const id=setInterval(load,15000);return()=>clearInterval(id)},[]);
 const market=useMemo(()=>universe.map(symbol=>{const t=tickers[symbol]||{};return{symbol,price:n(t.lastPrice),change:n(t.priceChangePercent),volume:n(t.quoteVolume),high:n(t.highPrice),low:n(t.lowPrice),signal:'WATCH',score:0,spike:0,trend5:'—',trend15:'—'}}).filter(x=>x.price>0).sort((a,b)=>b.volume-a.volume),[universe,tickers]);
 useEffect(()=>{let stop=false;async function deep(){const candidates=market.slice(0,30);const out=[];for(const r of candidates){try{const[k1,k5,k15]=await Promise.all([get(`/fapi/v1/klines?symbol=${r.symbol}&interval=1m&limit=30`),get(`/fapi/v1/klines?symbol=${r.symbol}&interval=5m&limit=30`),get(`/fapi/v1/klines?symbol=${r.symbol}&interval=15m&limit=30`)]);if(stop)break;out.push({...r,...analyse(k1,k5,k15,tickers[r.symbol]||{})})}catch{out.push(r)}}if(!stop){setRows(out.concat(market.slice(30)));setSignals(out.filter(x=>x.signal!=='WATCH').slice(0,20))}}deep();return()=>{stop=true}},[universe,tickers]);
 const toggle=m=>{setMode(m);localStorage.setItem('engine-mode',m)};
 const saveRisk=v=>{setRisk(v);localStorage.setItem('engine-risk',v)};
 return <main><header><div><div className="brand">⚡ BINANCE AUTO SCANNER</div><h1>All USDT Perpetuals · Live Momentum Engine</h1><p>Fresh engine · Binance USDⓈ-M Futures · every live USDT perpetual is scanned · prices stay live</p></div><div className="status"><b>● LIVE</b><span>{last?last.toLocaleTimeString():'Starting…'}</span></div></header>
 <section className="hero"><div><b>{universe.length}</b><small>TRADING COINS</small></div><div><b>{signals.filter(x=>x.signal==='BUY').length}</b><small>BUY SIGNALS</small></div><div><b>{signals.filter(x=>x.signal==='SELL').length}</b><small>SELL SIGNALS</small></div><div><b>{mode}</b><small>ENGINE MODE</small></div></section>
 <section className="control"><div><b>ENGINE</b><p>Paper mode is the default. LIVE sends real Binance Futures orders only after credentials and safety limits are configured on the server.</p></div><div className="buttons"><button className={mode==='OFF'?'on':''} onClick={()=>toggle('OFF')}>OFF</button><button className={mode==='PAPER'?'on':''} onClick={()=>toggle('PAPER')}>PAPER</button><button className={mode==='LIVE'?'danger':''} onClick={()=>toggle('LIVE')}>LIVE</button></div><label>Risk / trade % <input type="number" min="0.1" max="1" step="0.1" value={risk} onChange={e=>saveRisk(e.target.value)}/></label><label>Max positions <input type="number" min="1" max="5" value={maxPos} onChange={e=>{setMaxPos(e.target.value);localStorage.setItem('engine-maxpos',e.target.value)}}/></label></section>
 {err&&<div className="error">{err}</div>}
 <section className="panel"><div className="panelhead"><h2>Market Scanner</h2><span>{busy?'Refreshing…':`${rows.length||market.length} coins · 15s live refresh`}</span></div><div className="tablewrap"><table><thead><tr><th>#</th><th>COIN</th><th>PRICE</th><th>24H</th><th>24H VOLUME</th><th>5M</th><th>15M</th><th>SPIKE</th><th>SCORE</th><th>SIGNAL</th></tr></thead><tbody>{(rows.length?rows:market).map((r,i)=><tr key={r.symbol} className={r.signal==='BUY'?'buyrow':r.signal==='SELL'?'sellrow':''}><td>{i+1}</td><td><b>{r.symbol}</b></td><td>{fmt(r.price)}</td><td className={r.change>=0?'green':'red'}>{pct(r.change)}</td><td>{(r.volume/1e6).toFixed(1)}M</td><td className={r.trend5==='BULLISH'?'green':r.trend5==='BEARISH'?'red':''}>{r.trend5}</td><td className={r.trend15==='BULLISH'?'green':r.trend15==='BEARISH'?'red':''}>{r.trend15}</td><td>{r.spike?r.spike.toFixed(1)+'x':'—'}</td><td><b>{r.score||0}</b></td><td><span className={`pill ${r.signal?.toLowerCase()}`}>{r.signal}</span></td></tr>)}</tbody></table></div></section>
 <section className="grid"><div className="panel"><h2>Execution Rules</h2><ul><li>Universe: all active Binance USDT perpetual contracts.</li><li>Confirmation: 1m + 5m + 15m trend alignment.</li><li>Entry: momentum + volume expansion + multi-timeframe confirmation.</li><li>Initial risk: {risk}% of available engine equity per trade.</li><li>Maximum simultaneous positions: {maxPos}.</li><li>Default exit: ATR-based stop + 2R target + trailing protection.</li><li>Cooldown and duplicate-signal protection are enforced by the worker.</li></ul></div><div className="panel"><h2>Signal Log</h2>{signals.length?<div className="signals">{signals.map(s=><div key={s.symbol+s.signal}><b>{s.signal}</b> {s.symbol}<span>Score {s.score} · {s.spike.toFixed(1)}x</span></div>)}</div>:<p>No qualifying signal yet. The engine keeps scanning.</p>}</div></section>
 <footer>⚠️ LIVE mode is an execution feature, not a profit guarantee. Start with PAPER mode and verify fills, quantity filters, stops and risk limits before enabling real orders.</footer></main>}

createRoot(document.getElementById('root')).render(<App/>);
