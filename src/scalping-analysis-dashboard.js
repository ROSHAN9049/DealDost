import './scalping-analysis-dashboard.css';

const rootId='scalping-analysis-dashboard';
const n=v=>Number(v)||0;
const pct=v=>`${n(v)>=0?'+':''}${n(v).toFixed(2)}%`;
const money=v=>{const x=Math.abs(n(v));return x>=1e9?`$${(x/1e9).toFixed(2)}B`:x>=1e6?`$${(x/1e6).toFixed(2)}M`:x>=1e3?`$${(x/1e3).toFixed(1)}K`:`$${x.toFixed(0)}`};
const cls=v=>v==='BULLISH'?'bull':v==='BEARISH'?'bear':'neutral';
const esc=v=>String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;','\\':'&#39;'}[m]));
function parseScannerRows(){const out=[];document.querySelectorAll('.scanner-panel tbody tr').forEach(row=>{const cells=[...row.querySelectorAll('td')].map(td=>td.textContent.trim());const si=cells.findIndex(x=>/USDT\s*$/.test(x));if(si<0)return;const symbol=cells[si].match(/([^\s]+USDT)\s*$/)?.[1];if(!symbol)return;const price=n((cells[si+1]||'').replace(/[^0-9.eE+-]/g,''));const change=n((cells[si+2]||'').replace(/[^0-9.+-]/g,''));let vol=n((cells[si+3]||'').replace(/[^0-9.eE+-]/g,''));const vc=cells[si+3]||'';if(/B/i.test(vc))vol*=1e9;else if(/M/i.test(vc))vol*=1e6;else if(/K/i.test(vc))vol*=1e3;if(price>0)out.push({symbol,price,priceChangePercent:change,quoteVolume:vol})});return out}
function parseEngineCards(){const out=[];document.querySelectorAll('#scalping-engine-panel #scalp-list > div').forEach(card=>{const text=card.textContent.trim();const symbol=(text.match(/([A-Z0-9._-]+USDT)/)||[])[1];if(!symbol)return;const signal=/\b(BUY|SELL|WAIT)\b/.exec(text)?.[1]||'WAIT';const score=/Score\s+([0-9]+)/.exec(text);const spike=/Spike\s+([0-9.]+)x/.exec(text);const change=/24H\s+([+-]?[0-9.]+)%/.exec(text);const reason=text.split(/24H\s+[+-]?[0-9.]+%\s*/)[1]||'';out.push({symbol,signal,score:score?Number(score[1]):null,spike:spike?Number(spike[1]):null,priceChangePercent:change?Number(change[1]):0,reason:reason.trim()})});return out}
function getData(){const direct=Array.isArray(window.__BINANCE_SCALPING_ANALYSIS)?window.__BINANCE_SCALPING_ANALYSIS:[];const legacy=Array.isArray(window.__BINANCE_SCANNER_SIGNALS)?window.__BINANCE_SCANNER_SIGNALS:[];const ticks=window.__BINANCE_SCANNER_TICKERS||{};const base=direct.length?direct:legacy;const engine=base.length?base:parseEngineCards();const scanner=parseScannerRows();const map=new Map(scanner.map(x=>[x.symbol,x]));const signals=engine.map(x=>({...map.get(x.symbol),...x,price:n(x.price)||n(map.get(x.symbol)?.price),quoteVolume:n(x.quoteVolume)||n(map.get(x.symbol)?.quoteVolume)}));const merged=new Map(signals.map(x=>[x.symbol,x]));scanner.forEach(x=>{if(!merged.has(x.symbol))merged.set(x.symbol,{...x,signal:'WATCH',score:null,spike:null,trend1:'WAIT',trend5:'WAIT',trend15:'N/A',reason:'Waiting for rotating candle analysis'})});return{signals:[...merged.values()],ticks,engineActive:engine.length>0}}
function scoreRows(signals){return signals.sort((a,b)=>(b.score??-1)-(a.score??-1)||Math.abs(n(b.priceChangePercent))-Math.abs(n(a.priceChangePercent))).slice(0,20)}
function render(){let el=document.getElementById(rootId);if(!el){el=document.createElement('section');el.id=rootId;const root=document.getElementById('root');(root?.parentElement||document.body).appendChild(el)}const{signals,engineActive}=getData();const rows=scoreRows(signals),buys=signals.filter(x=>x.signal==='BUY'),sells=signals.filter(x=>x.signal==='SELL'),top=buys[0]||sells[0]||rows[0],ready=buys.length+sells.length;el.innerHTML=`<div class="scalp-head"><div><div class="scalp-kicker">⚡ BINANCE SCALPING MODE</div><h2>📊 LIVE ANALYSIS DASHBOARD</h2><p>1M momentum + 5M trend · 24H direction · volume spike · score engine · paper only</p></div><div class="scalp-live"><i></i> ${engineActive?'LIVE ANALYSIS':'WAITING FOR ENGINE'}</div></div><div class="scalp-kpis"><div><small>🟢 BUY READY</small><b class="bull">${buys.length}</b></div><div><small>🔴 SELL READY</small><b class="bear">${sells.length}</b></div><div><small>🎯 BEST SCORE</small><b class="score">${top?.score??'—'}</b></div><div><small>📡 READY SIGNALS</small><b>${ready}</b></div><div><small>🌐 MARKET COINS</small><b>${signals.length}</b></div></div>${top?`<div class="scalp-focus"><div><small>🔥 TOP SCALP CANDIDATE</small><strong>${esc(top.symbol)}</strong><span class="${top.signal==='BUY'?'bull':top.signal==='SELL'?'bear':'neutral'}">${top.signal==='BUY'?'🟢 BUY':top.signal==='SELL'?'🔴 SELL':'⚪ WATCH'}</span></div><div><small>PRICE</small><b>${n(top.price).toLocaleString('en-IN',{maximumFractionDigits:8})}</b></div><div><small>24H</small><b class="${n(top.priceChangePercent)>=0?'bull':'bear'}">${pct(top.priceChangePercent)}</b></div><div><small>VOLUME SPIKE</small><b>${top.spike==null?'—':`${n(top.spike).toFixed(2)}x`}</b></div><div><small>REASON</small><b>${esc(top.reason||'—')}</b></div></div>`:''}<div class="scalp-table-wrap"><table class="scalp-table"><thead><tr><th>#</th><th>COIN</th><th>PRICE</th><th>24H</th><th>VOL</th><th>1M</th><th>5M</th><th>15M</th><th>SPIKE</th><th>SCORE</th><th>SIGNAL</th><th>ANALYSIS</th></tr></thead><tbody>${rows.map((r,i)=>`<tr class="${r.signal==='BUY'?'row-buy':r.signal==='SELL'?'row-sell':'row-watch'}"><td>${i+1}</td><td><strong>${esc(r.symbol)}</strong></td><td>${n(r.price).toLocaleString('en-IN',{maximumFractionDigits:8})}</td><td class="${n(r.priceChangePercent)>=0?'bull':'bear'}">${pct(r.priceChangePercent)}</td><td>${money(r.quoteVolume)}</td><td class="${cls(r.trend1)}">${esc(r.trend1||'WAIT')}</td><td class="${cls(r.trend5)}">${esc(r.trend5||'WAIT')}</td><td class="neutral">${esc(r.trend15||'N/A')}</td><td class="${n(r.spike)>=1.5?'hot':''}">${r.spike==null?'—':`${n(r.spike).toFixed(2)}x`}</td><td class="${n(r.score)>=75?'score':''}">${r.score??'—'}</td><td><span class="sig ${r.signal==='BUY'?'sig-buy':r.signal==='SELL'?'sig-sell':'sig-watch'}">${r.signal==='BUY'?'🟢 BUY':r.signal==='SELL'?'🔴 SELL':'⚪ WATCH'}</span></td><td class="reason">${esc(r.reason||'Waiting for analysis')}</td></tr>`).join('')}</tbody></table></div><div class="scalp-foot"><span>🧠 Actual Paper Scalping Engine feed · 1M + 5M + 24H + volume spike</span><span>🛡️ Dashboard only · real orders remain OFF</span></div>`}
render();setInterval(render,1500);
